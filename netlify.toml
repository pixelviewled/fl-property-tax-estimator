# Netlify configuration for the Next.js app.
# Netlify auto-detects Next.js and installs the official runtime (@netlify/plugin-nextjs),
# which turns API routes into serverless functions. No extra setup needed.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
